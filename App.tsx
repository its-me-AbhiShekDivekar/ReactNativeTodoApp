import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import TodoStateProvider from './components/TodoStateProvider';

const App = () => {
  return <TodoStateProvider />;
};

export default App;

const styles = StyleSheet.create({});
